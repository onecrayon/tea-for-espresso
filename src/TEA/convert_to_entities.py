'''
Converts characters in selected (or the nearest character to the cursor)
into HTML entities
'''

import tea_actions as tea

def act(context, type='both'):
    '''
    Required action method
    
    Will convert into either named entities, numeric entities, or both
    '''
    pass
